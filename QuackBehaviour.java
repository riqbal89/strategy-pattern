package com.rifatiqbal.strategy_pattern;

public interface QuackBehaviour {
	
	/** 
	 * if the class is of type 'interface' then the abstract is implied automatically 
	 * no need to explicitly write it
	 */
	
	public void quack();

}
